let fields = []; // Array definieren

function fillShape(id) { // für jeden Kasten eine ID geben + 
    fields[id] = 'cross'; // an dieser Stelle entweder Circle oder Cross 
    console.log(fields); // zur Kontrolle - funktioniert es wirklich?
}